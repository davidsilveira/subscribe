#!/usr/bin/env ruby

require_relative '../lib/sales_taxes'

begin
  input = ARGF.read

  orders_items = SalesTaxes::InputParser.parse(input)
  receipt = SalesTaxes::Receipt.new(orders_items)

  puts receipt.call
rescue EOFError
  exit
end

